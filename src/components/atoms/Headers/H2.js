import styled from 'styled-components';
import baseHeaderStyles from './baseHeaderStyles';

const H2 = styled.h2`
  ${baseHeaderStyles};
  font-size: 36px;
  line-height: 40px;
`;

export default H2;
