import React from 'react';
import { storiesOf } from '@storybook/react';
import Button from '../../components/atoms/Button';

storiesOf('Atoms', module).add('Button', () => <Button>Button Text</Button>);
